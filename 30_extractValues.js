// Extract values from an object into an array

const obj = { name: "neha", id: 35, city: "kolhapur" };

let values = Object.values(obj);

console.log(values);

// Looping through values:
// Object.values(obj).forEach((value) => console.log(value));
